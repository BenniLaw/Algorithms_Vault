将无序列表调整为有序列表
- 输入: 列表
- 输出: 有序列表
排序分为升序和降序
内置函数:sort()

LowB三人组: 冒泡排序, 选择排序, 插入排序
NB三人组: 快速排序, 堆排序, 归并排序

# 1. 冒泡排序(原地排序)
- 列表每两个相邻的数，如果前面比后面大，则交换这两个数
- 每一趟排序完成后，会出来一个最大的数到有序区, 无序区减少一个数，有序区增加一个数
- 最后一趟不用做, 列表长度为n(有n个数), 只需要排n-1趟

- 关键点: 躺数, 无序区范围
	- 第0趟: 有序区0个数, 无序区n-0-1个数
	- 第1趟, 有序区1个数, 无序区n-1-1个数
	- 第i趟, 有序区i个数, 无序区n-i-1个数
	为什么还要-1, 因为被推到最上面的位置不用再与更上面(有序区)做比较, 所以做比较次数是无序区的个数(n-i)-1

![[Pasted image 20250920155517.png]]
```python
def bubble_sort(ls):
	for i in range(len(ls) - 1):
		for j in range(len(ls) - i - 1):
			if ls[j] > ls[j + 1]:    # 升序排列
			# if ls[j] < ls[j + 1]:  # 降序排列
				 li[j], li[j + 1] = li[j + 1], li[j]
```
改进: 如果在中途发生“没有发生任何交换”, 说明已经排序好
```python
def bubble_sort(ls):
	for i in range(len(ls) - 1):
		exchange = False.   # 每一趟都要检验是否有交换过, 新的一趟重置
		for j in range(len(ls) - i - 1):
			if ls[j] > ls[j + 1]:    # 升序排列
				 li[j], li[j + 1] = li[j + 1], li[j]
				 exchange = True
		if not exchange:
			return
```

# 2. 选择排序
## 2.1 简单选择排序
O(n)
```python
def select_sort_simple(li):
    li_new = []
    for i in range(len(li)):    # 因为不是原地排序, 所以必须全部都循环一次
		min_val = min(li)
        li_new.append(min_val)
        li.remove(min_val)
    return li_new
```
## 2.2 选择排序
找出来放最后/最前
和冒泡排序有种类似的感觉, 冒泡排序是前后作比较, 选择排序是单个和全部做比较
关键点: 无序区最小数的位置, 无序区的位置(每次遍历都是从无序区的第一个开始)
```python
def select_sort(li):
	for i in range(len(i) - 1):
		min_loc = i
		for j in range(i + 1, len(i)):
			if li[j] < li[min_loc]:
				min_loc = j
		li[min_loc], li[i] = li[i], li[min_loc]
```
没有别的改进优化方式

# 3. 插入排序
初始时手里(有序区)只有一张牌
每次(从无序区)摸一张牌，插入到手里已有牌的正确位置
```python
def insert_sort():
	for i in range(1, len(li))    # 摸n-1次/张牌, i表示每次摸的牌的下标
		j = i - 1    # j指手里的最后一张牌
		tmp = li[i]
		while li[j] > li[i] of j >= 0:    # 手里的牌比摸到的牌大
			li[j + 1] = li[j]
			j -= 1
		li[j + 1] = tmp    # 此时j指的是-1或需要放的位置的前一个
```

# 4. 快速排序
![[Pasted image 20250920201538.png]]
```python
def partition(li, left, right):
	tmp = li[left]    # 把第一个拿出来
	while left < right:
		while left < right and li[right] >= tmp:    # 从右边找比tmp小的
			right -= 1
		li[left] = li[right]    # 如果是第一种情况left == right导致的退出, 即没有找到比left更小的, 此时这个式子表示自己给自己赋值, 不变. 如果是第二种情况发现right🈶更小的, 就把更小的放到最边边的left
		while left < right and li[left] <= tmp:
			left += 1
		li[right] = li[left].   # 把左边的值放到右边刚刚空出来的空位
		
	li[left] = tmp    # 此时left和right碰头了, 是一个值

def quick_sort(li, left, right):
	if left < right:    # 保证至少有两个元素, 一个元素就是排完了
		mid = partition(li, left, right)
		quick_sort(li, left, mid - 1)
		quick_sort(li, mid + 1, right)
```
- 时间复杂度
- 问题
	- 递归本身的不好
	- 最坏情况: 复杂度会变成$n^2$

# 5. 堆排序
![[树与二叉树]]

